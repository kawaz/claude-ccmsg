# 裁定・確認待ち一覧 (ユーザ用)

> 運用規約 (ゼロコンテキスト読者向け、正本は claude-rules-personal の
> `questions-registry` skill):
> - 裁定待ち = ユーザの判断が要る項目。確認待ち = 実装済みで実機確認を待つ項目
> - ラベルはバッチ / セッション毎に一意な短プレフィクス (XX-Q1 / XX-C1 形式、
>   Qn 単独の使い回し禁止、長期一意性は不要)
> - 👺 は「いま待っている項目」とチャットの依頼文にだけ付ける
>   (裁定済み・過去参照に付けない。誤陽性がユーザのアラームを汚す)
> - チャット提示と同一ターンで本ファイルに記録 + path 指定 commit
>   (push はリリース窓に同乗)
> - 載せるのはユーザが今答えられる / 今確認できる形になったものだけ
> - 裁定が下りたら該当セクションを即削除し、内容は正規の記録先 (DR / issue /
>   journal / close_reason) へ反映。本ファイルは常に「現在待ち」だけを持つ
> - 確認待ちの削除条件: ユーザが確認 OK を返した / 後続リリースで当該実装が
>   上書きされ確認対象でなくなった
> - 1 項目 = 1 セクション、選択肢は `- [ ] a: …` のチェックボックス形式
>   (webui のプレビューからチェックして回答できる)
> - 参照パスはリポ内相対 (リポ外はフルパス)

## 裁定待ち

### 👺WEBUI-Q1: `<summary>` 無しの `<details>` のラベル (v0.73.31)

`<details>` に `<summary>` が無いときの見出し文言。現状は a で実装済み。

- [ ] a (現状): `詳細`
- [ ] b: `Details` (ブラウザ既定に寄せる)
- [ ] c: その他 (自由記入)

## 確認待ち

### 👺WEBUI-C2: TL の raw JSONL 表示 (v0.73.27 / v0.73.28)

- [ ] a: ツールバーの `raw` トグルで生 JSONL に切り替わる
- [ ] b: 行番号 + `@byte offset` の gutter が出る
- [ ] c: 長大行が切り詰められ「残り N 文字を表示」で全文展開できる

### 👺WEBUI-C3: FAB フォームが Terminal タブで閉じるか (v0.73.28)

- [ ] a: Terminal タブでフォームを開き、hyoui の iframe 内クリックで閉じる
- [ ] b: パネル右上の ✕ ボタンでも閉じる
- [ ] c: 閉じた後も書きかけテキストが保持されている

### 👺WEBUI-C4: markdown の `<details>` 折りたたみ (v0.73.31)

- [ ] a: 空行あり / なし両方の書き方で畳まれる
- [ ] b: `<details open>` が開いた状態で出る
- [ ] c: `<summary>` 内の `**bold**` が太字になる
- [ ] d: 閉じタグ無しや `<details onclick=...>` はテキストのまま (安全側)
- [ ] e: 枠線・余白の見栄えが許容範囲 (worker 側は未検証)

### 👺WEBUI-C5: Files 検索結果のお気に入り星 (v0.73.32)

- [ ] a: 検索結果行の右端の ☆ で登録できる
- [ ] b: ツリーの「お気に入り」セクションにも反映される
- [ ] c: 横スクロール時も星が画面内に留まる

### 👺WEBUI-C6: markdown の `<...>` がリンク化されない (v0.73.32)

- [ ] a: `<確認項目>` `<v0.73.31>` `<TODO>` が文字のまま出る
- [ ] b: `<https://...>` `<foo@example.com>` は従来どおりリンク

### 👺WEBUI-C7: markdown プレビューのチェックボックス操作 (v0.73.33)

このファイル自身が試験台です。上の項目をプレビューからチェックしてみてください。

- [ ] a: プレビュー上でチェックを付け外しでき、ファイルに反映される
- [ ] b: TL のメッセージ本文側のチェックボックスは操作できない (disabled)
- [ ] c: AI がファイルを書き換えた直後にチェックすると競合アラートが出る
