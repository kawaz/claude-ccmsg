# kawaz 発言: ID 体系 v2 (u/a 分離) の逐語 (2026-07-04)

一次資料。旧 ccmsg セッション `e17a9753-bc06-4d84-ab0c-bed806dfb6a5` の 2026-07-04 00:27〜00:40 の議論。
**この議論はセッション終了により DR / issue に一切記録されず消失していた** (2026-07-09 に kawaz の指摘でセッションログから発掘)。DR-0006 の根拠。

## kawaz 提案 (00:27)

> 作業進めてるとこ申し訳ない。メンバーについてu0がユーザで特別ルールをやめてエージェントメンバーはa:N,ユーザはu:Nにきっぱり分けた方が良いかなと思いはじめた。基本u1=僕(role=admin)しか居ない筈だが、webuiにゲスト招待みたいな感じでroや会話に参加出来るけど権限の弱いゲストユーザ(u2)が混じるロードマップが考えられる。当面不要だが。
> どう思いますか

## AI 応答の要点 (00:28、逐語は元ログ参照)

賛成 + 「dogfood 前 = jsonl にデータが積もる前の今が最適 (append-only 主義なので後からだと全 room migration)」。namespace 確保と role フィールドだけ入れ、ゲストの権限 enforcement は webui phase に先送りを推奨。

## kawaz 回答 (00:35)

> 良いと思う。
>
> {u:"u1"} {u:"aN"} と、
> {u:N} {a:N} とどちらが良い?

## kawaz 自己フォロー (00:39)

> すまんfrom区別考えたら後者は駄目だな。
> ここだけid文字列なのもどうかなと思ったんだが他もrNとかmNに揃えるでも良いのか。

## AI 最終具体案 (00:40) — **kawaz の返信なしにセッション終了、未確定**

- member id は型付き文字列 `u1` / `aN` (コロンなし)。ロケータ記法 (`#r7-m10` / `#u1`) とそのまま揃い変換層不要
- room id は `rN` 生成 (生成慣習であって形式ルールではない、クライアントは parse しない)
- **mid は protocol 上整数のまま** (連番ギャップ検出・範囲指定・大小比較という算術が本務。`mNN` はロケータ層の表記)
- 値が self-typing になるためフィールド名 `uid` → generic な `id` に戻す
- `u1` = admin (kawaz) 予約・全 room 暗黙参加 (現 0 と同じ挙動)。`u2+` = ゲスト、暗黙参加しない通常メンバー (`role:"guest"` 付き)、番号は room-local
- member イベントに `role` フィールド追加、権限 enforcement は将来 DR
- breaking change として version を上げる
